bugs:
- default match config / board config (when you click default on the preset) presets not actually setting automatically sometimes. this appeared to have happened after leaving / finishing a game and then creating a new lobby.
- when dice are network owned by a non-host player, the host does not hear the sounds of the dice.
- grand casino space name's chance image does not persist when duplicating the grand casino space names preset
- chat needs console polish treatment. fix: chats overlapping, chat does not auto scroll to bottom when opening

features:
- remove the "reset to defaults" button from the lobby match settings menu.
- add back buttons that sit next to the save/apply buttons on these menus: options, lobby customize, lobby match settings.
- add more turn state details to turn history page in integrity menu (turn has been skipped for "x reason", etc)
- add "pages" to move history (once the move history has reached x amount of moves, a new "page" will be generated. you can change pages by clicking buttons that auto generate on the bottom of the move history page. [1, 2, 3,...]) to avoid lag
- add game rule "can release properties" which adds another space action button "release" which has the action of mortgage but sends it to unowned status, only available if you own the property. default: false
- add more detail to post game stats (just tack on numbers to the already existing post game stats. ex: display amount of money earned for most earned stat, amount of money lost for most lost stat, etc)
- add new game rule: buffed utilities. when enabled, the multiplier is based on amount of spaces moved that entire turn rather than the roll. (this will interact with chance cards, if u land on a chance and it moves u to the utility, it will count total spaces moved [roll + moves from chance])
- lobby menu - add support for player join, leave, kick etc sounds. just add the sounds as placeholders in GameAssets, I will fill them in once you are done.