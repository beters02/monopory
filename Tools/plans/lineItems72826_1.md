- make it so you can right click a board property space to pull up a popup menu, for now the menu only contains: 
-- a header displaying the name of the property
-- an information label saying how many spaces you are away from it

- make sure close button is always on the same side (right side) of ui panels. you will have to comb through all UI panels/menus etc to see which ones currently have the close button on the wrong (left) side.

- midnight dice skin doesn't display the correct pip/dot color, although background color displays fine.

- post match screen still does not display enough information. the actual stats it displays are good, but it needs to display the relative number to that stat (eg Biggest Loser: {PlayerName} -${MoneyLost}). You should be able to rip these straight from the live game stats page.

- the host occasionally cannot hear dice sounds when other people are rolling the dice.