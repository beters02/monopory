- make it so stats screen doesn't get hidden during default roll

- display global popup for amount earned from free parking when a player lands (remove local popup if one exists)

- add "long standing" stats for: dice rolled numbers, spaces landed on numbers. these stats will be stored locally in your game files and can be accessed through a new command type "stat_" (stat_diceroll, stat_spaceland)

- fix styling on the "previous" and "next" buttons on the move history menu's page navigator

- make it so integrity button brings you to integrity page (currently it just opens up the stats menu exactly as stats button, it should open up specifically the integrity page within the stats menu)